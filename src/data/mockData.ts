import { Stock, AccountSummary } from '../types/stock'

export const mockHoldings: Stock[] = []

export const mockAccountSummary: AccountSummary = {
    totalPurchase: 0,
    totalEvaluation: 0,
    totalProfit: 0,
    profitRate: 0,
}

export const mockWatchlist: Stock[] = []
